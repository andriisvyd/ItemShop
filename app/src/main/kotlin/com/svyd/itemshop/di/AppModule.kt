package com.svyd.itemshop.di

import org.koin.dsl.module

/**
 * Top-level app-only bindings (e.g. things that depend on Application context
 * but don't belong to a specific feature).
 */
val appModule = module {
}
